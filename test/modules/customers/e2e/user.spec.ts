describe('', () => {
  afterAll(() => {});

  describe('GET /user', () => {
    it('should return one user', () => {});
    it('Should return an not found error', () => {});
    it('Should return an not authorize exception', () => {});
  });
});
