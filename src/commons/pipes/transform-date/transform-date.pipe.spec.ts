import { TransformDatePipe } from './transform-date.pipe';

describe('TransformDatePipe', () => {
  it('should be defined', () => {
    expect(new TransformDatePipe()).toBeDefined();
  });
});
